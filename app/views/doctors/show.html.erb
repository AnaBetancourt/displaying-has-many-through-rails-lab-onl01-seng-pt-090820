<h1><%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>

<h3>Appointments:</h3>
<% @doctor.appointments.each do |apt| %>
    <p><%= apt.datetime_readable %> - <%= link_to apt.patient.name, patient_path(apt.patient) %></p>
<% end %>